---
phase: 06-browser-rollout
type: validation
domain: telemetry
---

<objective>
Validate the Phase-6 host and Servo telemetry on real web workloads, confirm the selector-heavy ticks described in the plan, and capture the resulting diagnostics so the next iteration can refine instrumentation without weakening Phase-5 invariants.
</objective>

<context>
@.planning/phases/06-browser-rollout/06-01-PLAN.md
@.planning/phases/06-browser-rollout/06-02-PLAN.md
@docs/phase6_browser_ui.md
@docs/dropin_adapter_contract.md
@prototypes/browser-extension/host.js
@prototypes/browser-extension/telemetry.js
@prototypes/browser-extension/ui.js
</context>

<tasks>

<task type="development">
  <name>Task 1: Collect per-tick telemetry from real workloads</name>
  <files>prototypes/browser-extension/host.js prototypes/browser-extension/telemetry.js docs/phase6_browser_ui.md</files>
  <action>Install the Phase-6 prototype host/telemetry bundle on representative heavy pages (chatgpt.com, a dense news feed, the existing examples in `prototypes/dropin`). Record telemetry that includes tick identifiers, durations (script/style/layout/render), patch metadata, selector counters, fingerprint stability, and any guardrail fallbacks while the transactional mode remains feature-flagged on.</action>
  <verify>The telemetry stream captures at least one tick from each sample page with durations and patch metrics, selectors dominating the work stack, and guardrail status recorded (`running` vs `fallback`).</verify>
  <done>Sample logs (JSON/CSV) live under `.planning/phases/06-browser-rollout/telemetry-samples/` and are referenced by this plan.</done>
</task>

<task type="documentation">
  <name>Task 2: Record the telemetry story</name>
  <files>.planning/phases/06-browser-rollout/06-03-SUMMARY.md docs/phase6_browser_ui.md</files>
  <action>Summarize the collected telemetry samples, noting which phases dominated each tick, whether fingerprints remained stable, and how guardrail fallbacks behaved. Document how to interpret the data so contributors can use the same approach in future measurement runs.</action>
  <verify>The summary file captures tick highlights, selectors/script ratios, fallback triggers, and telemetry interpretation guidance; `docs/phase6_browser_ui.md` links to the summary.</verify>
  <done>Summary entry exists and points to the real telemetry files for easy inspection.</done>
</task>

<task type="coordination">
  <name>Task 3: Plan the next experiments</name>
  <files>.planning/STATE.md .planning/PROJECT.md</files>
  <action>Review the measurement summary, note any outstanding guardrail questions or instrumentation gaps (e.g., missing layout counters or guardrail health), and create follow-up TODOs or plan steps that keep Phase-5 semantics locked. Make sure upcoming work stays behind the Phase-6 feature flag and references `npm run conformance` expectations.</action>
  <verify>An updated `STATE.md` (or a new plan entry) records the outstanding telemetry/refinement actions, with links to this plan and the summary file.</verify>
  <done>Next-phase checklist exists so the team knows which telemetry/guardrail refinements to tackle once the current validation run is complete.</done>
</task>

</tasks>

<verification>
- [ ] Sample telemetry logs stored under `.planning/phases/06-browser-rollout/telemetry-samples/` cover at least two heavy pages.
- [ ] `06-03-SUMMARY.md` surfaces the selector-domain story and links the plan to the documentation.
- [ ] Any new follow-up actions are recorded in `.planning/STATE.md` (or referenced plans) with confirmation that guardrails/`npm run conformance` stay locked.
</verification>

<success_criteria>
- Real workloads emit the documented telemetry schema while the Phase-6 host remains engaged.
- Fingerprints remain deterministic across the sampled ticks and guardrail status only shifts when a fallback is intentionally triggered.
- The summary and follow-up plan make it obvious how to rerun and extend the telemetry experiment without weakening semantics.
</success_criteria>

<output>
This plan introduces `06-03-SUMMARY.md` for measurement highlights and keeps the telemetry story traceable from `docs/phase6_browser_ui.md` and the `.planning/STATE.md` tracker.
</output>
